spring:
  datasource:
    url: jdbc:h2:mem:cai_platform;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false

cai:
  identity-provider:
    base-url: http://localhost:8081
